# Use an official Nginx image to serve the frontend files
FROM nginx:alpine

# Copy the frontend files into the container's Nginx web directory
COPY . /usr/share/nginx/html

# Expose port 80 (Nginx default port)
EXPOSE 80

